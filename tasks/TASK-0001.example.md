---
task: TASK-0001
title: (example) "Export my data" on the settings page — delete this file at setup
owner: athena.example
status: ready
plan_ref: plan/(example)
repos: [front, backend]
branches: []
last_touched_by: athena.example
depends_on: []
blocked_by_ruling:
needs_ack: yes    # new export endpoint → contract; see contract/api-contract.md
loop_attempts: 0
last_failure: ""
created: 2026-01-01
---

## Goal

One-click JSON export of the user's own data from the settings page; backend provides an authenticated export endpoint.

## First slice

Draft the export-endpoint contract row and build the endpoint plus its two named tests on the task
branch. Starts with **nothing pending**: no ruling, no sibling task, no resource anyone else must
create — zeus's `[ack]` on the contract row gates the *merge*, not the start.

## Scope & boundaries

- In: export endpoint (own data only), settings button + download, empty-state.
- Out: scheduled exports, other formats, admin bulk export.
- **Allowed paths**: `backend/api/export/**` · `backend/tests/test_export*` · `front/src/pages/Settings/**`
- **Forbidden**: auth code paths; existing endpoints' behavior; `deploy/**`

## Notes

- Endpoint shape lands in `contract/api-contract.md` first, with zeus's [ack] (hence needs_ack);
- Strip internal fields from the export; reuse session auth;
- Frontend downloads via blob; storage paths never exposed.

## DoD

- [ ] merged into {{INTEGRATION_BRANCH}}
- [ ] diff stays inside Allowed paths (mechanical check)
- [ ] export returns own data only — proven by `test_export_excludes_other_users`
- [ ] internal fields stripped — proven by `test_export_redacts_internal_fields`
- [ ] front typecheck/tests/build green (real numbers)
- [ ] contract row co-signed
- [ ] finish letter sent to zeus.example

## Log

- 2026-01-01: created (example showing needs_ack and DoD style).
