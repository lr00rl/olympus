---
task: TASK-0001
title: (示例)给设置页添加"导出我的数据"入口 —— 实例化时删除本文件
owner: athena.example
status: ready
plan_ref: plan/(示例)
repos: [front, backend]
branches: []
depends_on: []
needs_ack: yes    # 新增导出端点 → 契约;见 contract/api-contract.md
created: 2026-01-01
---

## 目标

用户在设置页一键导出自己的全部数据为 JSON;后端提供带鉴权的导出端点。

## 范围与非目标

- 做:导出端点(仅本人数据)、设置页按钮与下载、空数据空态。
- 不做:定时导出、其他格式、管理员批量导出。

## 实施要点

- 端点形状先进 `contract/api-contract.md` 并拿 zeus [ack](needs_ack 的来源);
- 导出内容剔除内部字段;权限沿用现有会话鉴权;
- 前端下载用 blob,不暴露存储路径。

## DoD

- [ ] 代码合并进 {{INTEGRATION_BRANCH}}
- [ ] backend 测试 + front tsc/vitest/build 全绿(报真实数字)
- [ ] 契约条目与变更单已会签
- [ ] finish 信已发给 zeus.example

## 日志

- 2026-01-01: 创建(示例任务,展示 needs_ack 与 DoD 的写法)。
